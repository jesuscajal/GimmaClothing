# Nginx + Cloudflare (SSL Full strict con certificado de origen).
#
# 1. Reemplazar TU_DOMINIO.com por tu dominio real.
# 2. Colocar el certificado de origen de Cloudflare en:
#    /etc/ssl/cloudflare/TU_DOMINIO.com.pem
#    /etc/ssl/cloudflare/TU_DOMINIO.com.key
# 3. sudo ln -s /ruta/al/repo/deploy/nginx/gimma.conf /etc/nginx/sites-enabled/
# 4. sudo nginx -t && sudo systemctl reload nginx

# IPs de Cloudflare para obtener la IP real del visitante
# Lista actualizada: https://www.cloudflare.com/ips/
set_real_ip_from 103.21.244.0/22;
set_real_ip_from 103.22.200.0/22;
set_real_ip_from 103.31.4.0/22;
set_real_ip_from 104.16.0.0/13;
set_real_ip_from 104.24.0.0/14;
set_real_ip_from 108.162.192.0/18;
set_real_ip_from 131.0.72.0/22;
set_real_ip_from 141.101.64.0/18;
set_real_ip_from 162.158.0.0/15;
set_real_ip_from 172.64.0.0/13;
set_real_ip_from 173.245.48.0/20;
set_real_ip_from 188.114.96.0/20;
set_real_ip_from 190.93.240.0/20;
set_real_ip_from 197.234.240.0/22;
set_real_ip_from 198.41.128.0/17;
set_real_ip_from 2400:cb00::/32;
set_real_ip_from 2606:4700::/32;
set_real_ip_from 2803:f800::/32;
set_real_ip_from 2405:b500::/32;
set_real_ip_from 2405:8100::/32;
set_real_ip_from 2a06:98c0::/29;
set_real_ip_from 2c0f:f248::/32;
real_ip_header CF-Connecting-IP;

# Redirigir HTTP → HTTPS (Cloudflare también puede forzar HTTPS)
server {
    listen 80;
    listen [::]:80;
    server_name TU_DOMINIO.com www.TU_DOMINIO.com api.TU_DOMINIO.com;
    return 301 https://$host$request_uri;
}

# Tienda (Next.js) — dominio principal
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name TU_DOMINIO.com www.TU_DOMINIO.com;

    ssl_certificate     /etc/ssl/cloudflare/TU_DOMINIO.com.pem;
    ssl_certificate_key /etc/ssl/cloudflare/TU_DOMINIO.com.key;
    ssl_protocols       TLSv1.2 TLSv1.3;

    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }
}

# API Medusa + panel admin (/app)
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name api.TU_DOMINIO.com;

    ssl_certificate     /etc/ssl/cloudflare/TU_DOMINIO.com.pem;
    ssl_certificate_key /etc/ssl/cloudflare/TU_DOMINIO.com.key;
    ssl_protocols       TLSv1.2 TLSv1.3;

    client_max_body_size 50M;

    location / {
        proxy_pass http://127.0.0.1:9000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }
}
